PORT=3000

# Filled in during the build stage once architecture (storage choice, ingestion source) is
# decided in DOCS/DECISIONS.md — e.g. a DB connection string if Supabase is chosen, or a
# NAPIX/data.gov.in API key for the ingestion script if that path is used server-side.
