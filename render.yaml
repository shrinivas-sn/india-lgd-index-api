services:
  - type: web
    name: india-lgd-index-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm ci && npm test && npm run check:snapshot
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
